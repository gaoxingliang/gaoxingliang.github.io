---
layout: post
title: "Python virtualenv 使用方法"
date: 2018-11-09 17:51:21 +0800
author: gaoxingliang
description: "Source: https://docs.python-guide.org/dev/virtualenvs/1. 确保python 已经安装2. 安装virtualenvpip install virtualenv3.检查是否安装成功virtualenv --version4. 为测试项目创建一个virtualenv:cd my_project_foldervi..."
categories:
  - 迁移自CSDN
tags:
  - "virtualenv"
  - "python"
csdn_url: "https://blog.csdn.net/scugxl/article/details/83901152"
---

Source: <https://docs.python-guide.org/dev/virtualenvs/>

1. 确保python 已经安装

2. 安装virtualenv

```
pip install virtualenv
```

3.检查是否安装成功

```
virtualenv --version
```

4. 为测试项目创建一个virtualenv:

```
cd my_project_folder
virtualenv my_project
```

  为virtualenv 指定独立的python:

```
virtualenv -p /usr/bin/python2.7 my_project
```

5. 激活virtualenv:

```
source my_project/bin/activate
```

然后你就可以在里面任意安装软件了:

```
pip install requests
```

6. 退出:

```
deactivate
```

这样你就可以很容易的同时运行python2 和 python 3了
